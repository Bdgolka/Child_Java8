package com.bdgolka.lambda;

/**
 * 
 * @author Pulova Uses one functional interface with three different lambda
 *         expression
 * 
 *         Interface takes two parameters int and return result type boolean
 */
interface NumericTest {
	boolean test(int n, int m);
}
